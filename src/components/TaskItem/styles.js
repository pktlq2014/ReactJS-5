// css in js
const styles = (theme) => ({
    box: {
        display: "flex",
    },
    shape: {
        backgroundColor: theme.color.primary,
        padding: "2rem",
        marginRight: "1rem",
        color: theme.shape.textColor
    },
    card : {
        
    }
});
export default styles;